# Python tooling for linting and running the playbook, pinned so CI and your
# workstation behave identically.
#   python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements-dev.txt
ansible-core==2.21.4
ansible-lint==26.9.0
yamllint==1.38.0
