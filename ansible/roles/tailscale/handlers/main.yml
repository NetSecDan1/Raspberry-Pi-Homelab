---
- name: Restart tailscale-udp-gro
  ansible.builtin.systemd_service:
    name: tailscale-udp-gro.service
    state: restarted
    daemon_reload: true
  when:
    - tailscale_udp_gro_forwarding
    - not (ansible_check_mode and tailscale_gro_unit is changed)
