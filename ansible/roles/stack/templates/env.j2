# {{ ansible_managed }}
# Rendered by Ansible from roles/stack/templates/env.j2 -- edits here are
# overwritten on the next deploy. Keys documented in docker/.env.example.
TZ={{ stack_timezone }}
# Single quotes = taken literally by compose (no $ interpolation).
# So the password itself must not contain a single quote.
PIHOLE_WEBPASSWORD='{{ stack_pihole_password }}'
